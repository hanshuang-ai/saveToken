# 通用 Token 节省插件 —— 设计文档

> 形态:完整架构(愿景)+ MVP 规格(只做通用核心,不加适配器)
> 技术栈:Bun/TypeScript + SQLite(FTS5)
> 参考:项目内 `token-save-projects.md`、`Token节省思路全景报告.md`

---

## Context(背景)

现存 100+ 个开源 token 节省方案**全部偏向编码场景**,且各自只解决一类内容。用户的痛点正是"不好使、不通用、只偏向编码"。本设计要做一个**内容感知的通用核心 + 可插拔场景适配器**,先落地通用核心验证可行性,再逐步加适配器。

差异化定位:**没人做"按内容类型自动切换策略"的通用核心**——这是空缺,也是本项目的价值锚点。

---

## 〇、范围与边界(定稿)

### 管什么:压"进上下文的一切"

本插件压的是**送进上下文的内容**,不是只压工具输出。输入往往比输出多,是 token 大头。

| 路 | 是什么 | 谁产生 | 压不压 | 为啥 |
|---|---|---|---|---|
| **进上下文** | 工具返回的结果、读的文件、贴的长内容、长日志 | 工具/用户 | ✅ 压 | token 大头,最该压 |
| **出上下文** | 模型生成的回复 | 模型 | ❌ 不压 | 那是它想说的,压它伤质量 |
| **对话历史** | 聊了很久的旧轮次 | 系统 | ⚠️ 单独做,默认关 | 见下"一个坑" |

### 一个坑:对话压缩 ≠ 工具输出压缩

- 工具输出/读文件压缩:**一字不差还原**(无损,我们能保证)
- 对话历史压缩:通常**变成要点**(有损,还原不了)

这俩混在一起会出事。所以对话压缩**单独开关、默认关**,跟无损压缩分开。MVP 不做对话压缩。

### 不做的(明确边界)
- 不压模型回复(输出)
- 不加"简洁输出"提示词全局默认开(无差别简洁会砍代码注释/错误细节/该展开的说明,伤质量)
- 不做去空白压缩(token 不按字符算,空格常并入相邻词不单独计费;且易误伤代码缩进)
- 不全局强制极简文风(那伤编码质量)

### 三条技术选型(定稿)

1. **存原件 → 本地文件数据库**:所有层(检索/备份/度量/记忆)共用一个 SQLite 文件,零基础设施,能存能查。
2. **压缩原语自写**:现成压缩库(fflate/pako/lz-string)压出来是不可读乱码,模型读不懂,不能进上下文;只能用于存储备份压缩。无损压缩原语(截断/去噪/反向引用)自己写,可逆可控。
3. **分类器自写**:查遍 npm 与 PyPI,**没有任何库解决"这段文本是结构化还是散文还是混合"这件事**。
   - JS 候选:linguist-js(333KB,只识别编程语言)、franc/languagedetect(只识别人类语言,代码英文标识符会被误判成英语→把代码当散文,危险)、text-readability(只算英文可读性)
   - Python 候选:guesslang(挂 TensorFlow,2-12MB+框架,每次推理几百毫秒,热路径灾难)、Pygments(词法高亮非分类)
   - 共同结论:所有库都在"识别是哪种语言",没人做"结构化度判断"。能借鉴算法思路(关键字表/trigram),但不背依赖。
   - 信号须通用,不依赖任何标记格式:符号密度、行结构相似度、信息熵、自然语言 trigram 命中率、token 长度分布。**去掉偏 Markdown 的信号(代码围栏 ```、标题 #)**——这些对 LaTeX/纯文本/HTML/日志不通用。

---

## 一、为什么现在都不通用(结构性根因)

省 token 技术天然分两派,各自只适合一类内容:

| 派别 | 机制 | 适合 | 致命限制 |
|---|---|---|---|
| **无损/启发式** | 去噪、反向引用、截断、格式压缩 | 结构化/冗余(CLI输出、JSON、代码) | 对散文无效——没有可剥离结构 |
| **有损/语义** | 小模型删冗余 token、摘要 | 任意文本 | 有损、需模型(慢/贵),prism 研究证明小模型上反而亏 |

所以每个项目都只选一派 + 一个领域(RTK=无损+编码,LLMLingua=有损+RAG)。**没人做内容分类路由**。这就是空缺。

---

## 二、完整架构总览(愿景)

```
                    ┌─────────────────────────┐
  进上下文的内容 ──→ │  内容分类路由器(核心)   │  ← 工具结果/读文件/粘贴内容/长日志
                    └────────────┬────────────┘
              ┌──────────────────┼──────────────────┐
              ▼                  ▼                  ▼
       结构化/冗余           语义散文              混合
   (CLI/JSON/日志/代码)    (文档/书籍/对话)
              │                  │
              ▼                  ▼
   无损启发式压缩        检索优先(不加载,按需查)
   + 检索存储            + 可选语义压缩(opt-in)
              │                  │
              └────────┬─────────┘
                       ▼
                 记忆层(跨会话) + 度量层(含任务成功率)
```

### 第一层:通用核心(内容无关)—— MVP 范围
1. **内容分类路由器**:判断进入上下文的是结构化/散文/混合,派发策略。**新核心,无人做过。**
2. **无损原语**(对任意文本安全):截断(head+tail)、反向引用、格式剥离、紧凑序列化。从不损害质量,默认开启。压缩内容磁盘留备份,可取回原文。
3. **检索存储**(SQLite + FTS5/BM25):**通用最大杠杆不是压文本,而是根本不加载**。索引一次,按需查相关片段。对代码和文档都适用,且无损。
4. **度量层**:同时记 token 节省 **和 任务成功率**(回应 prism 反向研究)。

### 第二层:场景适配器(可插拔)—— MVP 之后
- **编码适配器**:CLI 去噪(RTK 式)、AST 索引(graphify 式)、think-in-code(context-mode 式)、只传 diff。
- **文档/阅读适配器**:语义分块、渐进式披露(索引→摘要→详情)、query-aware 压缩(opt-in)。**差异化所在,无人做。**
- **对话适配器**:旧轮次摘要、语义去重。
- **写作适配器**:**不过度简洁**的输出引导(尊重 context-mode"强制简洁损害质量"教训)。

---

## 三、Claude Code 接线

| 扩展点 | 干什么 | 层 | 状态 |
|---|---|---|---|
| `PostToolUse` hook | 拦截工具结果 → 分类 → 无损压缩 → `updatedToolOutput` 替换为压缩版(对模型透明) | 核心压缩 | ✅ 已接线实测 |
| `PreToolUse` hook | 有索引时,把 Read/Grep 重定向到检索 MCP | 检索层 | 待做 |
| `UserPromptSubmit` hook | ⚠️ 不能替换 prompt,只能 `additionalContext` 追加——用户粘贴长内容的压缩此路不通 | 核心压缩 | 受限 |
| MCP server | 暴露检索 + 度量工具(`tok_retrieve`/`tok_stats`) | 检索/度量 | 待做 |
| Skill(按场景加载) | 场景感知输出引导,非全局强制简洁 | 输出层 | 待做 |
| `PreCompact` hook | 注入压缩记忆而非丢上下文 | 记忆层 | 待做 |
| `SessionStart` hook | 只加载最小索引,不加载全量 | 记忆层 | 待做 |

### PostToolUse 契约(已实测,来源 code.claude.com/docs/en/hooks)

- **输入** stdin JSON:`{ tool_name, tool_input, tool_response, cwd, session_id, ... }`
- **输出** stdout JSON:`{ hookSpecificOutput: { hookEventName: "PostToolUse", updatedToolOutput: <压缩版> } }`
- **`updatedToolOutput`**:在工具输出发送给模型**之前**替换它。值必须匹配工具的输出形状(Bash 是 `{stdout, stderr, interrupted, ...}`)。
- **关键**:工具到 hook 触发时**已执行**(副作用已发生:文件已写、命令已跑),`updatedToolOutput` 只改"模型看到的",不改"工具实际做的"——正好符合需求,无需自行重新执行工具。
- **适配策略**:递归遍历 `tool_response`,只压缩长字符串字段、结构原样保留 → 形状自动匹配任何工具,无需逐工具硬编码。
- **`UserPromptSubmit` 不能替换 prompt**(只能 `additionalContext` 追加)——用户粘贴内容的压缩需另寻路径(如引导用专用命令,或接受追加)。

关键设计:压缩内容磁盘留**备份原文**,模型需要时可取回(Caveman 的 backup-on-disk 模式)——透明且可逆。

### Hook 接线实测结果(任务 #20)

`hooks/post-tool-compress.ts` + `hooks/hooks.json`(matcher: `Bash|Read`):
- 137199 字符结构化日志 → 压缩到 2245(**98.4%**),形状自动匹配
- 原文 `getOriginal(orig-1) === 原始 stdout` 严格相等(安全模型第三道闸门)
- FTS5 检索可用,`tok_retrieve` 能按关键词查片段
- 度量正确记录(节省比/类型/handle)
- 散文/短输出/非白名单/异常 全部放行(四道安全闸门)
- 全量 34 测试通过,未破坏现有模块

---

## 四、技术栈

- **核心**:Bun/TypeScript(Claude Code hook/MCP 是 JS 友好,ashlr 已验证 Bun 可行)。
- **存储**:SQLite 内置 FTS5——context-mode 验证过,零基础设施。
- **语义检索**:可选接 Ollama 本地 embedding,**不作必需依赖**(MVP 不含)。

---

## 五、三条红线(现有项目教训)

1. **无损优先,有损 opt-in**——默认可逆压缩,从不损害质量(prism 警告)。
2. **检索优于压缩**——通用场景赢面是"不加载"而非"压文本",且无损。
3. **度量必须含任务成功率**——否则造出"看起来省、实际亏"的插件。

---

## 五·甲、Hook 拦截策略(默认放行)

hook 默认"不作为",只有满足全部条件才动手压缩。双层过滤:

### 维度一:工具白名单
| 工具 | 拦不拦 | 理由 |
|---|---|---|
| `Bash` | ✅ 拦 | 最大浪费源,git log/find/npm 动辄几千行 |
| `Read` | ✅ 拦 | 大文件全读进上下文 |
| `Grep`/`Glob` | ⚠️ 看情况 | 匹配多时大,少时没必要 |
| `Edit`/`Write`/`Task`/`TodoWrite` | ❌ 不拦 | 输出极小,拦了纯亏 |
| MCP 工具 | ⚠️ 配置 | 按需加入 |

拦 Bash + Read 即覆盖 80%+ 浪费。

### 维度二:内容类型 + 大小阈值
| 内容类型 | 压不压 | 理由 |
|---|---|---|
| 结构化/冗余(CLI日志、JSON、重复内容) | ✅ 压 | 有可剥离冗余,可逆无损 |
| 散文/文档/对话 | ❌ 不压 | 无可剥离结构,碰了有失真风险 |
| 混合 | ⚠️ 分段处理 | 只压其中结构化部分 |
| < 阈值(如 <2KB) | ❌ 不压 | 太小,压了反而增加开销 |

### 拦截决策流程
```
工具输出 →
  1. 工具在白名单?      否→原样返回
  2. 输出 > 阈值?       否→原样返回
  3. 内容=结构化?       否(散文/混合)→原样返回(或仅压结构化段)
  4. 原文存 SQLite 备份
  5. 可逆压缩 → 返回压缩版 + handle
  6. 记度量
```
四道关卡,任一不满足即放行。白名单与阈值均可配置,可随时关掉某个工具。

---

## 五·乙、语义零失真安全模型(四道闸门)

```
第零道:工具白名单 + 内容类型 + 大小阈值 → 绝大多数直接放行
第一道:散文/文档不碰(hook 对叙事内容等于不存在)→ 源头杜绝
第二道:只做可逆变换 + 截断部分存盘(延迟加载,非删除)
第三道:原文永远备份在 SQLite,handle 可取回原文 → 兜底
```

### 关键区分:压缩 ≠ 摘要
| | 摘要(有损,我们不做) | 压缩(无损,我们只做) |
|---|---|---|
| 机制 | 小模型删"不重要"内容 | 规则变换,信息完整 |
| 能还原 | 不能 | 能,严格相等 |
| 语义风险 | 高 | 零(原理上不可能失真) |

### 不压"语义关键内容"
即使对结构化内容,以下永远不压(借鉴 Caveman 边界):
代码、命令、文件路径、精确错误信息、数字、模型决策依赖的关键字段。
只压"结构性冗余":重复 ANSI 码、超长日志中间段、重复字符串。

### 截断的特殊说明
截断看似有损,实则"延迟加载":省略部分存进磁盘索引,handle 是指针。
模型需要时 `tok_retrieve(handle)` 取回**原文**。信息没丢,只是延后。
与摘要的"删了就没了"本质不同。

### 验证无损
1. **往返单测**:每种原语 `解压(压缩(x)) === x`,fuzz 随机输入
2. **集成 A/B**:同任务开/关 hook,对比**任务结果**一致(prism:测每成功任务 token)
3. **取回率监控**:模型频繁取回 = 压缩太激进,该调阈值

---

## 六、MVP 规格(只做通用核心)

### 范围
✅ 内容分类路由器 + 无损原语 + 检索存储 + 度量层
❌ 不做任何场景适配器 / 不做有损语义压缩 / 不做记忆层

### 目录结构(标准 Claude Code 插件形态)
```
saveToken/                          插件根 = marketplace 仓库
├── .claude-plugin/
│   ├── plugin.json                 插件清单(name=frugal)
│   └── marketplace.json            本地 marketplace(source=".")
├── commands/
│   └── status.md                   /frugal:status 诊断面板(已完成)
├── src/
│   ├── diagnose.ts                 诊断脚本(已完成)
│   ├── core/
│   │   ├── classifier.ts           内容分类路由器(扩展名快筛+二进制检测+内容启发式三层)
│   │   ├── ext-filter.ts           扩展名/文件名快筛层(决策库反推)
│   │   ├── decision-log.ts         分类决策日志(积累决策库,持续进化)
│   │   ├── dispatcher.ts           策略派发
│   │   └── types.ts
│   ├── compress/
│   │   ├── snip.ts                 head+tail 截断
│   │   ├── dedup.ts                反向引用
│   │   ├── format-strip.ts         ANSI/空白剥离
│   │   └── index.ts                无损原语聚合
│   ├── store/
│   │   ├── db.ts                   SQLite 存储层(原文备份+FTS5检索+度量,已完成)
│   │   ├── index.ts                存储与检索导出
│   │   └── schema.sql              表结构(原文表+FTS5+度量表)
│   ├── metrics/
│   │   ├── logger.ts               token/成功率记录
│   │   └── report.ts
│   └── mcp/
│       └── server.ts               MCP server 入口
├── hooks/
│   └── hooks.json                  PostToolUse 拦截配置
├── tests/
│   ├── classifier.test.ts
│   └── round-trip.test.ts          无损往返测试
├── package.json
└── tsconfig.json
```

### 各模块规格

**1. 内容分类路由器 `classifier.ts`**(已完成,三层架构,真实数据实测)
- 输入:内容字符串 + 元数据(工具名、mime、路径 path)
- **三层判定**(有路径时快筛优先,无路径才内容兜底):
  1. **扩展名/文件名快筛**(`ext-filter.ts`,决策库反推):有路径时第一档扩展名(.yml/.log/.html/.json/.css/.ts...)或文件名关键词(main_log/kernel_log/cpuinfo...)直接判 structured,不读内容。`.yml` 实测 91% 被内容误判散文,扩展名兜底解决。第二档(.md/.txt/.pug/.styl)给提示但仍内容细判。
  2. **二进制检测**:含 NUL 或前 2KB 控制字符>10% → structured(.gz 解压乱码、MTK scp 日志)。堵住乱码落兜底被判 prose 的漏洞。
  3. **内容启发式信号**(无路径/二档/无线索时兜底):强符号密度 `{};`、标记标签 `<tag>`、行结构相似度(排除列表行)、JSON 合法性、ANSI、自然语言密度(中文虚词+全角标点+英文虚词\b词边界)、连续散文段(中文强信号,防乱码误触发)。
- 输出:`{ type: 'structured'|'prose'|'mixed', confidence, signals }`
- 派发:structured→无损原语+索引;prose→放行(不压);mixed→仅 format-strip(保守,不碰散文段)
- **安全侧倾斜**:散文绝不误判 structured。
- **实测**(BLOG 1486文件/298纯散文 + 车载日志包551文件/870MB):
  - 散文误判率 31.9% → 0.3%(真散文 0,剩余为文档内联代码无损无害)
  - 日志包结构化覆盖率 86% → 100% 字节(扩展名快筛解决最大 mixed 漏判)
- **决策日志**(`decision-log.ts`):每次判定记 JSONL(路径/扩展名/文件名关键词/判定/置信度/信号),积累决策库。已积累 13167 条,反推扩展名表三档分类。后续 hook 运行时持续记录,数据驱动进化。
- 测试方法:`tests/evaluate.ts` 全量扫描真实项目,出混淆矩阵 + 散文误判率(安全侧硬指标)+ ground truth 净化。详见记忆 classifier-test-method

**2. 无损原语 `compress/*`**
- `snipCompact`:超长输出截成 head+尾 + 省略中间标记(ashlr 式)
- `dedup`:重复串替换为反向引用(foveance 式),可逆
- `formatStrip`:剥离 ANSI 码/多余空白(Deblank 式),可逆
- 全部**可逆**,压缩后原文写盘备份

**3. 检索存储 `store/`**(已完成,第一阶段)
- SQLite + FTS5 + BM25,三职责统一:原文备份表 + 全文索引 + 度量表
- `saveOriginal`/`getOriginal`:存原文/取回(handle 取回严格相等,安全模型第三道闸门)
- `search(handle, query)`:FTS5 关键词检索,返回原文切片带行号(供 tok_retrieve)
- `recordMetric`/`getMetricSummary`:度量记录与汇总(节省比、分类型、取回率)
- snip 截断中间段已接入 store(内存 Map 回退 + SQLite 持久化),截断内容可检索取回
- **第一阶段边界**:只服务无引用的结构化内容(日志/JSON/表格)。代码只压缩不检索(见七·甲 TODO-1)
- 测试:`tests/store.test.ts` 13 项(存取一致、检索命中、度量汇总、取回计数、边界),全量 34 测试通过

**4. 度量层 `metrics/`**
- 每次记录:原始 token、压缩后 token、节省比、内容类型、所用策略、handle
- 任务成功率信号:后续是否重复请求原文 / 用户是否重试 / 是否在合理轮次内完成
- 报表报"每成功任务 token",不只报"总节省"

### 接线(MVP 只接最关键两个)—— 已完成并实测
- `PostToolUse` hook(`hooks/post-tool-compress.ts`)→ 分类 → 无损压缩 → `updatedToolOutput` 替换工具输出(原文备份) → 记度量。实测 13.7万字符日志压到 2245(98.4%),原文严格可取回。
- MCP server(`src/mcp/server.ts`)→ `tok_retrieve`(取原文/检索片段/按行取)+ `tok_stats`(节省比/分类型/取回率)。已注册到 `plugin.json` 的 `mcpServers`。端到端 stdio 协议实测通过:检索命中带行号、按行取正确、取回计数累加(压缩激进度信号)。

### 验证方式
- **分类器单测**:标注好的 fixtures(JSON/log/code/prose/mixed 各若干),断言分类正确
- **无损往返测**:compress → decompress 严格等于原文
- **集成**:在 Claude Code 跑,`/tok_stats` 显示节省 + 内容类型分布
- **A/B**:同样任务开/关插件,同时量 token **和** 成功率,确认没"省了但搞坏任务"

---

## 七、路线图(MVP 之后)

1. 文档/阅读适配器(差异化最大,且无损检索对它最有效)
2. 编码适配器(CLI 去噪 / AST 索引 / think-in-code / diff-only)
3. 记忆层(跨会话压缩召回)
4. 对话适配器(旧轮次摘要、去重)
5. 有损语义压缩(opt-in,全程度量成功率)

---

## 七·甲、关键 TODO(分阶段决策记录)

> 以下为分阶段实施时明确"暂不做但后续必须做"的项,记录背景与决策依据,避免后续遗漏。

### TODO-1:代码 AST 图检索(扁平检索验证后接手)

**背景**:存储层检索替代全量加载是省 token 最大杠杆(红线2)。但代码有引用关系(`import`/调用/继承),扁平全文检索会断引用——模型取 `bar()` 实现时,它调用的 `foo()` 在别处,扁平检索不知道这个关系,返回的是断片,模型拿到残缺信息反而判断错。

**决策**:分阶段。
- **第一阶段(MVP 存储层)**:扁平全文检索 + FTS5,**只服务无引用的结构化内容**(日志/JSON/表格)。代码暂时**只压缩不检索**(给全文或无损压缩全文,不搞检索),避免断引用。
- **第二阶段(编码适配器)**:代码走 AST 图检索(像 codegraph,tree-sitter 解析语法树建图,取符号时连带取引用)。这是独立大模块,扁平这套验证稳了再做。

**触发条件**:扁平检索(日志类)在真实使用中验证稳定后,启动代码 AST 检索开发。

**参考**:codegraph(68k★,tree-sitter + 图数据库,已验证代码索引路线可借鉴)、设计文档第二层"编码适配器"。

