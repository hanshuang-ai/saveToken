# 《AI Agent in Depth》深度分析报告

> 分析对象:AI-Agents-in-Depth-zh-CN.epub(中文版)
> 分析方式:5 轮递进式分析,本文件逐轮追加
> 配套代码:https://github.com/bojieli/ai-agent-book

---

## 第1轮:书籍结构概览

### 一、全书定位与核心公式

本书源自作者(李博杰,Pine AI 首席科学家)2025 年图灵《AI Agent 实战营》课程讲义,经系统整理而成。定位是**以可解释、可验证、可复用的工程原则设计 AI Agent**,而非跑通演示。

**核心公式(一句话贯穿全书):**

```
Agent = LLM + 上下文 + 工具
```

三层等价表述:

| 直觉层 | 实现层 | 学术层(RL 形式化) |
|---|---|---|
| 大脑 | LLM | Policy(策略) |
| 眼睛 | 上下文 | Observation Space(观察空间) |
| 手脚 | 工具 | Action Space(动作空间) |

作者强调一个贯穿性观点:**"实践在前,命名在后"**——Skill、Harness、Loop Engineering 等概念在流行之前,Pine 等团队已在用相应方法解决问题。

### 二、章节结构图

全书 10 章,围绕两个相互衔接的问题组织:

```
┌─────────────────────────────────────────────────────────────┐
│            第一部分:如何构建 Agent(第 1-6 章)              │
├─────────────────────────────────────────────────────────────┤
│  Ch1 AI Agent 入门                                          │
│      └→ 建立全局认知:核心公式 + ReAct 循环 + 编排模式       │
│  Ch2 上下文工程 ★全书最关键                                 │
│      └→ API 结构→KV Cache→提示工程→提示注入→Skills→        │
│         状态栏→上下文压缩                                    │
│  Ch3 用户记忆和知识库                                        │
│      └→ 上下文延伸到跨会话:记忆四策略→RAG 全栈→            │
│         智能体化 RAG→多模态记忆                              │
│  Ch4 工具                                                    │
│      └→ Agent 与外部交互:MCP 标准+五类工具设计原则          │
│  Ch5 Coding Agent 与通用 Agent                               │
│      └→ 代码+文件系统=通用 Agent 技术基础(OpenClaw 主线)   │
│  Ch6 交互:观察与动作空间的扩展                              │
│      └→ 异步事件驱动→语音→Computer Use→机器人操作          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼ 能力提升
┌─────────────────────────────────────────────────────────────┐
│         第二部分:如何提升 Agent 能力(第 7-10 章)          │
├─────────────────────────────────────────────────────────────┤
│  Ch7 Agent 的评估                                            │
│      └→ 可度量反馈:评估环境+数据集+LLM-as-Judge+选型闭环   │
│  Ch8 模型后训练                                              │
│      └→ 参数层提升:SFT/RL 全景+奖励设计+多轮 RL+蒸馏      │
│  Ch9 Agent 的持续进化                                        │
│      └→ 经验转能力:学习信号→四种更新载体→进化闭环          │
│  Ch10 多 Agent 协作                                          │
│      └→ 群体系统:分类框架+协作模式+Agent 社会与经济        │
└─────────────────────────────────────────────────────────────┘
```

**两部分逻辑衔接:** 第一部分解决"把 Agent 造出来"(架构),第二部分解决"让 Agent 变强"(从评估→参数→单体经验→群体协同四个层面扩展)。第七章评估是第二部分的前提——"没有评估,就无法判断改动是真实提升还是随机波动"。

### 三、核心概念清单(按全书出现频次排序)

基于跨章全文检索的术语频次,量化各概念权重:

| 概念 | 频次 | 所属层 | 释义 |
|---|---|---|---|
| **评估** | 320 | 方法论 | 贯穿全书,第七章专论;迭代从经验判断→可复现工程的基础 |
| **Harness** | 210 | 架构 | 模型之外的工程层;解决工具调用不稳定、幻觉、越权等问题 |
| **Skill** | 195 | 机制 | 领域能力的可组合单元;动态加载提示词解决提示词无限膨胀 |
| **SFT** | 162 | 训练 | 监督微调,"照样学样";与 RL 形成"记忆 vs 泛化"对照 |
| **上下文压缩** | 144 | 上下文 | 不只是长度问题;隔离优于压缩(子 Agent 上下文隔离) |
| **RAG** | 88 | 知识 | 检索增强生成;从扁平检索到智能体化 RAG(自主决定何时检索) |
| **多 Agent** | 81 | 协作 | 第十章;上下文共享/独立 × 对等/管理者/去中心化 |
| **MCP** | 75 | 工具 | 工具互操作标准;工具生态与 Skill Hub |
| **KV Cache** | 68 | 推理 | 复用历史计算;缓存作为架构约束,影响上下文设计 |
| **状态栏** | 67 | 上下文 | Agent 感知执行环境/用户时间的元信息增强 |
| **Computer Use** | 39 | 交互 | GUI 自动化;动作空间扩展到图形界面 |
| **强化学习** | 35 | 训练 | RL;让模型通过试错和奖励反馈自主提升 |
| **VLA** | 33 | 机器人 | Vision-Language-Action 控制;机器人操作 |
| **提示注入** | 31 | 安全 | 上下文安全的核心威胁 |
| **ReAct** | 25 | 架构 | 思考→行动→观察的迭代循环,Agent 核心运行机制 |
| **提议者-审核者** | 25 | 架构 | 解决模型过早认为任务完成;loop engineering 实践 |
| **上下文工程** | 24 | 上下文 | 第二章主题;决定 Agent 能力上限的关键 |
| **世界模型** | 16 | 机器人 | 仿真环境到真实机器人的桥梁 |
| **自举** | 12 | 进化 | Agent 通过生成代码实现自我进化;创造智能体的智能体 |

### 四、关键术语关系网

```
                    Agent = LLM + 上下文 + 工具
                         /        |         \
                   大脑/策略    眼睛/观察    手脚/动作
                       |          |            |
                    ReAct 循环 ←──┴──→ 工具系统
                    (思考-行动-观察)      ├ MCP 标准
                       |                ├ Skill(动态加载)
                  Harness 工程           └ 五类工具
              (模型之外的竞争力)              |
                       |              交互扩展
              ┌────────┼────────┐    (Ch6)
           提示工程  状态栏  上下文压缩   异步/语音/Computer Use/机器人
              |
          提示注入(安全威胁)
```

### 第1轮小结

- 全书以 `Agent = LLM + 上下文 + 工具` 为轴,10 章分"构建(1-6)"与"提升(7-10)"两大部分
- 概念权重揭示重心:**评估(320)、Harness(210)、Skill(195)** 是三大高频核心,说明本书重方法论与工程架构,非纯理论
- 作者立场鲜明:"实践在前命名在后",原则来自长流程高风险场景(Pine 的账单协商/退款等真实经济任务)
- 章节相对独立,作者建议:开发者顺序读;时间紧读 Ch1+Ch2;训练方向读 Ch7+Ch8

---

## 第2轮:技术体系分析

> 聚焦第一部分「如何构建 Agent」(第1-6章),重点第1章(架构总览)与第2章(上下文工程/提示工程)

### 一、技术架构图:Agent 的双层结构

全书架构骨架是两个等价公式,分别对应不同抽象层次:

```
              Agent = LLM + 上下文 + 工具          (工程视角)
                        ↕ 等价展开
              Agent = Model + Harness              (生产视角)

  ┌─────────────────────── Agent 边界 ───────────────────────┐
  │                                                          │
  │   Model(LLM/大脑)          Harness(模型之外的治理层)    │
  │   - 策略决策               ┌──────────────────────────┐  │
  │   - 理解/规划/执行         │ 上下文管理  ← Ch2/Ch3    │  │
  │   - 内部思考(reasoning)   │ 工具接口    ← Ch4        │  │
  │                           │ 约束(Constrain)← Ch4    │  │
  │                           │ 验证(Verify)  ← Ch5/Ch6  │  │
  │                           │ 纠正(Correct) ← Ch2/Ch5  │  │
  │                           └──────────────────────────┘  │
  │                                   ↕ 观察/行动接口        │
  └───────────────────────────────────┼──────────────────────┘
                                      ▼
                          Environment(环境,边界之外)
                  文件/数据库/网页/用户/其他Agent/物理世界
```

**关键洞察:** 最小 Demo 只需 Model + 能构造上下文/暴露工具的 Harness;生产系统才加入约束、验证、纠正。Claude Code 的 Harness 中**绝大部分代码是约束/验证/纠正,而非工具本身**——"能做事"→"可靠地做事"的演进。

### 二、ReAct 循环:核心运行机制

```
   ┌─────────────────────────────────────────┐
   │  上下文 = 静态前缀 + 轨迹(trajectory)   │
   │  静态前缀 = 系统提示词 + 工具定义        │
   │  轨迹 = 用户消息 + 模型回复 + 工具结果   │
   └─────────────────────────────────────────┘
                     │
                     ▼
   ┌─── ReAct 循环(想→做→看)──────────────┐
   │  1. 思考(Reasoning):当前该做什么       │
   │  2. 行动(Acting):调用工具             │
   │  3. 观察(Observation):看工具返回       │
   │     ↓ 循环直至无工具调用 → 返回答案     │
   └──────────────────────────────────────┘
```

每次 LLM 调用看到完整轨迹(只增不改),因此清楚进展到哪、试过什么。轨迹可沉淀进知识库或用于 RL 训练,形成学习闭环。

### 三、Agent 架构模式总览

#### 3.1 工程范式演进(层层包含,非替代)

```
提示工程 ⊂ 上下文工程 ⊂ Harness工程 ⊂ Loop工程 ⊂ Graph工程
 (优化指令)  (管理可见信息) (模型之外治理) (跨轮次自主) (执行图编排)
```

LangChain 实例:Terminal Bench 2.0 得分 52.8%→66.5%,改的不是模型而是 Harness(自动检查结果、检测循环、优化思考策略)。**模型能力商品化时,竞争优势转移到模型之外的工程。**

#### 3.2 编排模式:工作流 vs 自主 Agent

| 维度 | 工作流(Workflow) | 自主 Agent(Autonomous) |
|---|---|---|
| 执行路径 | 预定义代码固定 | Agent 实时动态决定 |
| 优势 | 严格流程控制、攻击面限单节点 | 灵活应对未预见情况 |
| 局限 | 缺乏变通,异常走预设分支 | 成本高、复合错误风险 |
| 适用 | 合规严格的固定流程(订票付款) | 开放问题(Coding/Computer Use/研究) |

实践多混合:关键合规用工作流,灵活决策切自主。Manus 原则:**"Less structure, more intelligence"**——守住边界,把边界内决策空间还给模型。

#### 3.3 护栏三层(按被绕过难度排序,越下越难穿透)

```
上下文层护栏 ── 管模型能看什么(进上下文前拦截)
  相关性/安全分类器、内容审核、规则过滤、来源标注
  代表:Anthropic Constitutional Classifiers(规则驱动+上下文联合判断+两级筛查)
  结构性上限:同一上下文里的Agent难判断自己是否被注入 → 只能降低,给不出保证
       │
执行层护栏 ── 管模型能做什么(动作生效前验证)
  工具风险评级、独立审查进程、最小权限、沙盒、人在回路
  关键:复核必须由上下文之外的机制完成,否则与被注入Agent同沦陷
       │
数据层护栏 ── 管世界最终被改成什么样(强制执行)
  行级安全策略、约束校验器、受控视图、受信任运行时
  价值:不依赖上两层是否正确——提示注入得手、代码漏写权限,越权仍被拒
```

#### 3.4 五大设计模式(贯穿全书)

| 模式 | 核心 | 出现章节 |
|---|---|---|
| 提议者-审核者 | 产出与评判分由不共享上下文的两角色承担;自审不可靠 | Ch3/4/5/7/9/10 |
| 渐进式披露 | 可检索目录常驻,细节按需加载 | Ch2(Skills)/3/4/10 |
| 只增不改 | 状态追加演进,已写不回头→可缓存可重放可审计 | Ch2(KV Cache)/3/4 |
| 边界集+保留集 | 修改同时验证"该改的"和"不该影响的"样本 | Ch7/8/9 |
| 最小diff+可回滚 | 改动小、带来源、可单独回滚→可归因 | Ch3/5/9 |

### 四、提示工程最佳实践(第2章核心)

**核心对象:** 系统提示词(`role: system`),Agent 的"员工手册"。**检验标准:聪明新员工读完还不知道怎么做,Agent 也一样不知道。**

#### 4.1 六个维度

**① 语气与风格(人格)**
- 大写强调(`NEVER do X`)比 `Please avoid` 更引起模型注意,但保留给真正关键的约束,过度使用会稀释
- 无法完成时控制 1-2 句,避免冗长自我辩护
- 消融实验:风格对完成率影响有限(模型风格适应力强)

**② 结构化提示(格式)— 影响最大**
- XML 标签携带精确语义(`<working_directory>`),Markdown 组织可读层次,双层配合
- **消融实证:信息组织打乱(去标题、拆流程为无序规则)→ 成功率降超 30%**,常违反关键业务规则(如跳过身份验证直接退款)
- 原则:对人类友好的组织方式,对模型同样友好

**③ 流程驱动 vs 规则堆砌(组织方式)**
- 零散规则堆砌→多条同时适用时困惑、未覆盖时无措
- **流程驱动=SOP**,模型任何时刻知道处于哪个阶段、目标、下一步
- 把上百条规则组织成 Step1→Step2→...的标准操作流程

**④ 业务规则细化(内容)— 最易忽视最关键**
- 产品经理设计规则(非工程师),明确到可执行程度
- **不给模型业务规则上的自由裁量权**——模糊规则("根据情况选择")导致行为不可预测
- Pine 计费案例:按提成/按小费/预收款三模式,明确"NEVER use percentage_based for refunds"(退款绝不按提成)
- 成功率映射计费(>60%可退款、<30%拒绝)、计费粒度写死、防"把避免未来涨价算成省钱"

**⑤ Few-shot 示例(何时给例子)**
- 难以规则描述的(风格、格式、语气分寸)→ 给 2-3 个示例,胜过等量抽象规则
- 模型本擅长且规则易说清的 → 示例只是浪费 token
- **字节级稳定**:示例在上下文靠前,一旦确定不应逐请求动态挑选(否则 KV Cache 持续失效);为每类任务准备固定示例集
- 数量:2-3 个覆盖边界情况 > 10 个大同小异(后者稀释对规则的注意力)

**⑥ 工具定义设计**
- 每个工具描述含:使用边界、具体示例、性能提示、工具间协作关系
- Claude Code 实例:"NEVER invoke grep as Bash"、"Batch your tool calls"、"Use Read before editing"
- **消融实证:移除工具描述文本 → 错误率 +45%**,频繁传无效参数
- 渐进式披露演进(2026 原生能力):OpenAI `tool_search`+`defer_loading`、Anthropic Tool Search、Codex BM25 tool_search——静态前缀只留名称简述,完整 schema 按需追加到末尾(不破坏 KV Cache,因因果注意力只依赖之前 token)

#### 4.2 提示注入:上下文安全核心威胁

- 本质:外部内容(网页/邮件/文档)伪装系统指令劫持 Agent
- Agent 比聊天机器人更危险——有工具调用,可执行删除/发邮件/泄露隐私等不可逆操作
- 攻击面随能力增长:每个感知工具都是注入入口(网页不可见元素、PDF 元数据、图片 EXIF)
- Skill/状态栏也构成新注入面——第三方 Skill 内容如藏恶意指令比网页隐藏文本更直接;安装前必须审查(如同审查代码)

### 第2轮小结

- 架构双公式:`Agent = LLM+上下文+工具`(工程)≡ `Agent = Model+Harness`(生产),Harness 五要素决定可靠性
- ReAct 是运行机制,轨迹=静态前缀+动态消息,只增不改
- 竞争力已从模型转向 Harness(约束/验证/纠正),工程范式五层递进
- 提示工程六维度,**结构化组织影响最大**(打乱降 30%),业务规则需产品经理细化到可执行、不给模型裁量权
- 安全是架构问题:护栏三层(上下文/执行/数据),越靠下越难穿透

---

## 第3轮:工程实践分析

> 聚焦第二部分「如何提升 Agent 能力」(第7-9章):评估、后训练、持续进化
> 核心命题:没有评估,改动提升与随机波动不可区分;评估是第二部分的前提

### 一、评估框架(第7章):可度量反馈的工程化

#### 1.1 双重定位:Pass@k 技术上限 vs Pass^k 业务可靠

```
Pass@k = P(k 次生成至少 1 次成功)   → 测模型上限 / 技术炫技
Pass^k = P(k 次生成全部成功)         → 测业务可靠 / 长尾失败
```

| 指标 | 含义 | 适用 |
|---|---|---|
| Pass@k | 单次理想成功率 | 选模型、看上限、研究报告 |
| Pass^k | 可靠执行概率 | 生产系统、长流程高风险任务 |

**业务取舍:** 账单协商一次失败即真实经济损失,生产看 Pass^k 而非 Pass@k。技术 demo 与生产可靠是两套指标体系。

#### 1.2 评估环境五要素 + 验证器为核心

五要素:任务集 / 环境 / 动作接口 / 验证器 / 指标。**验证器决定"什么算成功",是五要素的核心。**

- **OSWorld 案例:** 134 个验证函数,每个任务专属断言——验证逻辑与任务深度耦合,非通用判分
- **外部独立原则:** 验证不能靠模型自报完成。长程 coding/cowork 任务须把"是否完成"交给**模型写不了的隐藏测试、状态断言、外部终止钩子**,而非模型声称完成
- 结构性上限:同一上下文里的 Agent 难判断自己是否被注入 → 验证须由上下文之外机制完成(呼应第2轮护栏三层)

#### 1.3 LLM-as-a-Judge:自动化评估核心(7.5 节)

**为什么需要:** 开放式任务(生成报告、处理投诉、创意内容)无标准答案可自动对比,人工评估成本高且难规模化。LLM-as-a-Judge 在自动化规模与人类专业判断间取得平衡。

**本质 = Rubric + Judge 两件工具:**
- Rubric 把笼统"好不好"拆成若干可分别打分的维度
- LLM-as-Judge 在缺乏确定性判据时按 Rubric 打分
- 二者合用,把一个笼统失败率**还原为可着手修复的具体问题**

**已知局限——长度偏差:** 评判模型倾向给更长更详尽的回复打高分("蒙对"一两个点)。三种防范:

| 手段 | 做法 |
|---|---|
| Rubric 惩罚 | 显式惩罚冗长 + 同类任务规定长度上限 |
| 配对比较 | 先把两候选长度控制相近再评 |
| 定期审计 | 审计评分与回答长度相关性;高分总伴长答→被带偏→回炉修订 Rubric |

**扩展维度:** 多源评判(多个评判模型降低单模型偏见)+ 多模态 LLM-as-Judge(能力从文本扩展到语音/图像/视频,提升覆盖度)

#### 1.4 失败归因 + 轨迹回流闭环

**失败任务同样有分析价值。** AndroidWorld 案例:用户号码 555-123-2002,Agent 却选 L1001 线路;`get_details_by_id(L1001)` 明确返回号码 555-123-2001,**Agent 读取结果却未修正判断**,在无关排查上耗数十条消息后转人工。失败形态归类:证据已进入上下文,Agent 未据此回溯。

**生产轨迹回流闭环:**
```
线上真实失败用例(用户纠正 / 点踩 / 规则验证器 / LLM 评审发现)
        ↓ 失败归因
   沉淀为回归用例(成本最高,准确性也最高)
        ↓
   端到端回归 + 轨迹前缀回归
        ↓ 迭代
```

#### 1.5 数据集质量:泄漏防范 + 参数化生成

- 公开基准数据泄漏:用 **canary GUID** 嵌入检测是否混入训练数据
- 参数化生成:避免硬编码,用模板生成大量变体防过拟合
- 公开基准分数难直接指导产品决策,但其设计手法(验证深度、参数化生成、泄漏防范、质量维护)可迁移到自建评估集

### 二、训练流程(第8章):SFT 记忆、RL 泛化

#### 2.1 四阶段全景(教育类比)

| 阶段 | 类比 | 做什么 |
|---|---|---|
| 预训练 | 通识教育 | 海量语料学语言与世界知识 |
| Mid-training | 集中研修 | 工具/推理/长上下文等能力集中训练 |
| SFT | 老师示范 | 标注示范轨迹,照样学样 |
| RL | 自己下场做题 | 试错 + 奖励反馈,自主提升 |

#### 2.2 两大主线论点

1. **"SFT 记忆、RL 泛化"**:SFT 把示范轨迹记进参数(模仿上限=数据覆盖率);RL 探索新路径泛化能力(可超越示范)
2. **"数据和环境比算法更重要"**:算法决定如何更新参数,奖励决定什么算成功,**环境和数据决定模型能经历哪些问题**——三者中数据/环境是能力上限的真正约束

#### 2.3 SFT 本质:换了数据的预测下一个词

- 不是新机制,就是 next-token prediction,数据换成标注示范
- **loss masking:** 只对示范部分计算 loss,提示部分不算(否则模型学"复述提示"而非"产生示范")
- "记忆"含义:把示范轨迹的概率推高

#### 2.4 SFT vs RL 本质区别(协变量漂移是分水岭)

| 维度 | SFT | RL |
|---|---|---|
| 信号来源 | 示范轨迹 | 环境 + 奖励 |
| 覆盖上限 | = 数据覆盖率 | 可超越示范 |
| 训练前缀 | 给定正确前缀 | 自有(可能犯错)轨迹 |
| **协变量漂移** | **训练用正确前缀,推理时自己生成错误前缀→错误累积→分布外** | 在自有轨迹训练,天然抗漂移 |
| 擅长 | 把已有低概率成功推高 | 同左 |
| 不擅长 | — | 从全零创造新能力(需 SFT 冷启动) |

**关键洞察:** RL 擅长把已有低概率成功推高,不擅长从全零创造。SimpleVLA-RL 用一条演示做 SFT 冷启动,再 RL 把成功率从 17.3% 推到 91.7%——冷启动提供"已有低概率",RL 负责推高。

#### 2.5 奖励设计:三维度 + 路径合规(RLVP)

```
奖励设计 = 来自哪里 × 什么时候给 × 表达多少信息  +  结果正确时路径是否合规
```

**维度一:奖励来自哪里**
- **可验证奖励 RLVR**(测试用例/DB 断言/状态差异/格式检查)——最可靠、最便宜、最可复现、最不易被钻空子
- 人类偏好
- 模型评判

**维度二:什么时候给**

| 类型 | 时机 | 特点 | 案例 |
|---|---|---|---|
| 结果奖励 ORM | 终点 | 最大探索自由;稀疏难定位具体错误 | SimpleVLA-RL(稀疏成功/失败) |
| 过程奖励 PRM | 中间步骤 | 缓解长时序信用分配;限预设路径、标注成本高 | V-IRL(逐步导航反馈) |

- **对照:** 密集反馈换收敛速度 vs 稀疏反馈换探索空间
- **工程策略:** 先用结果奖励建可靠基线,再只为真正可验证的中间事件加入过程信号

**维度三:表达多少信息:** 二元 → 标量 → 向量 → 路径

**路径合规(RLVP):** 结果正确还不够,路径要合规——不能绕过验证直接拿答案。GRPO 组内比较中,全成功/全失败组相对优势消失,RLVP 的路径信号/过程奖励/部分进展奖励正是为恢复这种差异而设计。

#### 2.6 GRPO 组内比较

```
16 条采样,4 条通过 → 组均成功率 4/16
  通过 4 条:高于组均 → 正优势
  失败 12 条:低于组均 → 负优势
全成功 / 全失败 → 奖励相同 → 比较不出 → 相对优势消失(需 RLVP 恢复)
```

- PPO 价值网络负责把终点反馈归因到较早动作
- GRPO 把轨迹级优势均摊到生成 token——**长轨迹信号稀释**需格外注意
- 多轮 LLM RL 通常折扣因子 γ=1

### 三、持续进化(第9章):经验转能力

#### 3.1 学习信号:验证器输出的四项结构化诊断

单一总分只反映一次运行优劣,无法指出应改何处。**可供后续学习的评价至少包含四项:**

| # | 内容 |
|---|---|
| 1 | 任务是成功、部分成功还是失败 |
| 2 | 每个维度各自的结论 |
| 3 | 每条结论对应的证据位置(哪一轮对话、哪一次工具调用) |
| 4 | 失败的类型标签 |

- 验证器应被允许**在证据不足时拒绝评分**——低置信度案例排除在学习集之外,而非当作事实固化
- 具备这四项,后续才能判断应更新知识、提示词、程序还是参数

#### 3.2 四种更新载体(经验 → 能力的落地路径)

学习信号说明"应改变",四种载体说明"改变发生在哪里":

| 学习内容性质 | 更新载体 | 成本 | 速度 |
|---|---|---|---|
| 事实和经验 | 知识文档 | 低 | 即时 |
| 可语言表达的策略 | Prompt / Skill | 低 | 即时 |
| 可精确执行的流程与约束 | 程序 / Harness | 中 | 快 |
| 感知/语言风格/隐式策略等高维能力 | 模型参数(SFT/RL) | 高 | 慢 |

- **选择原则:** 能用便宜载体解决就不用贵的;参数更新是最后手段
- **不互斥:** 医疗影像 Agent = 参数识别病灶 + 知识库提供最新指南 + 代码算风险指标;客服 = 参数自然语气 + 知识/Skill 企业政策 + 服务端代码合规兜底

#### 3.3 进化闭环 + 睡眠学习

```
新版本 → 验证 → 灰度发布 → 回滚 → 长期整理
```

- 类似离线训练周期:**睡眠学习**五步——收集运行经验 → 生成训练数据 → 离线训练 → 验证 → 上线
- 守住安全边界:经验转能力不能突破安全约束(合规变通 = 原方案不可行时找允许的替代路径,而非绕过规则)

### 第3轮小结

- **评估框架:** Pass@k 上限 vs Pass^k 可靠双指标 → 五要素环境(验证器为核心,外部独立) → LLM-as-Judge(Rubric 拆维度 + Judge 打分,防长度偏差三招) → 失败归因 + 轨迹回流闭环 → 数据泄漏防范(canary GUID + 参数化生成)
- **训练流程:** 四阶段(预训练/Mid/SFT/RL) → 双主线(SFT 记忆 RL 泛化;数据环境 > 算法) → SFT 本质(换数据的 next-token + loss masking) → SFT vs RL(协变量漂移 T² 是分水岭,RL 推高非创造) → 奖励三维(RLVR 最优 / ORM vs PRM 探索-收敛权衡 / 信息量)+ 路径合规 RLVP → GRPO 组内比较
- **持续进化:** 学习信号 = 验证器四项结构化诊断(含证据位置 + 拒评机制) → 四种更新载体(便宜优先,不互斥) → 进化闭环(验证/灰度/回滚)+ 睡眠学习 → 安全边界
- **三章逻辑链:** 评估(知道改没改)→ 后训练(改模型参数)→ 持续进化(把运行经验转成四种载体的能力更新),评估贯穿始终

---

## 第4轮:案例与应用分析

> 横向梳理全书案例与实现,聚焦第4-6章(工具/Coding Agent/交互)的工程实践

### 一、案例汇总(贯穿全书的产品/系统)

| 案例 | 章节 | 类型 | 验证/说明的问题 |
|---|---|---|---|
| **Pine / PineClaw** | 引言/6 | 生产系统 | 代替用户拨真实电话(账单协商/退款/取消订阅/保险理赔),长流程高风险真实经济任务;通话中随时需用户介入 |
| **Manus** | 1/5 | 通用 Agent | "Less structure, more intelligence";Deep Research+Computer Use+Coding 融合 |
| **Claude Code** | 2/4/5 | Coding Agent | Harness 范本——绝大部分代码是约束/验证/纠正而非工具;工具描述最佳实践 |
| **OpenClaw** | 5 | 通用 Agent | Coding Agent + 文件系统架构主线;MEMORY.md + Markdown 日志;Heartbeat 主动服务 |
| **LangChain** | 1 | 框架 | Terminal Bench 2.0 52.8%→66.5%,改 Harness 非模型 |
| **OSWorld** | 7 | 评估环境 | 134 个验证函数,每任务专属断言——验证器深度耦合 |
| **AndroidWorld** | 7 | 评估环境 | 失败归因案例:证据已进上下文 Agent 未回溯 |
| **SimpleVLA-RL** | 8 | RL 实验 | 一条演示 SFT 冷启动 + RL,17.3%→91.7%,发现"推切"动作;稀疏结果奖励换探索空间 |
| **V-IRL** | 8 | RL 实验 | 逐步导航过程奖励——密集反馈换收敛速度(与 SimpleVLA-RL 对照) |
| **AdaptThink/ReTool** | 8 | RL 算法 | 自定义约束目标/PPO——算法决定怎么比,奖励决定什么算成功 |

### 二、Coding Agent 实现(第5章主线)

#### 2.1 核心架构:Coding Agent + 文件系统

**核心论断:** 以开放任务为目标的通用 Agent,核心是一个 Coding Agent 加文件系统。从 Manus 到 OpenClaw 都遵循此范式。

**为什么 Coding 是核心(而非 Deep Research / Computer Use):**
- 几乎所有高效内容生成最终落到代码——PPT/Word 本质是 OOXML 代码,PDF 可由 Markdown/HTML/LaTeX 生成,数据分析由 Python 脚本完成
- GUI 操作序列可固化为可复用代码;Deep Research 的搜索综合可由代码驱动 Web 请求实现
- Computer Use 通用性更强,但**成本、延迟、稳定性远不如直接代码/API**
- 代码生成是效率最高、成本最低、可复用性最强的能力基座

#### 2.2 代码生成:通用 Agent 的元能力

**元能力定义:** "能创造其他能力"的能力——当场写出新工具、新约束、新表达形式,不必事先预制。代码精确、可执行、可组合,故能产出新工具(脚本/API 序列)、新约束(断言/校验)、新表达形态(HTML/PPT/视频帧)。

**六个应用方向(按"作用对象"由内向外):** 数学思考 → 内容创作 → … → **系统接口**(连接机器与机器,无现成 SDK/接口不规范时当场读文档生成适配代码——HTTP 客户端/鉴权头/解析非标准返回/数据模型转换,代码成"万能胶")。

**适用边界:**
- 开放任务通用 Agent(深度调研/内容生成/数据处理)→ Coding 是架构核心
- 垂直客服 Agent(任务封闭)→ Coding 是工具箱里一件工具,非中枢;但精确计算/数据处理/规则校验仍离不开它

#### 2.3 文件系统:选 Markdown 而非向量数据库

OpenClaw 中文件系统是记忆、知识和能力的中枢(MEMORY.md + 按日期归档 Markdown 日志)。选 Markdown 而非向量 DB 的三理由:

| 理由 | 说明 |
|---|---|
| 可读可改 | 用户可直接打开阅读修改;Agent 记错某事,直接删那一行即可 |
| 时间顺序 | 天然保留时间顺序,避免语义检索中的时间混淆 |
| 可版本控制 | 可通过 Git 版本控制和回滚 |

### 三、工具设计与安全边界(第4章)

#### 3.1 五类工具 + 两个独立决策

五类:感知 / 执行 / 协作 / 事件触发 / 用户沟通(按"调用方向"+"作用对象"两特征审视)。

**两个独立决策(易混为一谈):**
- **形态**(专用工具 vs Skill):定每条能力常驻多少 token、参数怎么传、谁能改
- **披露**(一次让模型看见多少):定有多少条同时摆在模型面前
- 四种组合都真实存在(几百工具的 MCP 后端可只暴露索引按需加载;Skill 目录可全量常驻也可分层检索)

#### 3.2 通用工具 vs 专用工具

**默认取向:通用工具优于专用工具**(除非明确的安全/权限/性能理由)。code_interpreter 装好 sympy/numpy/pandas 即"元能力",一个解释器代替数十个特定工具,还能处理预想不到的边缘场景。

**四种情况退回专用工具:**
1. 安全/权限/审计——生产 DB 写操作需精细权限控制和审计粒度(开放 code_interpreter 做不到)
2. 屏蔽平台差异——grep/find 跨平台语法不同,专用工具给清晰行号反馈
3. 高频操作——值得专属入口
4. 参数结构复杂——嵌套对象/多字段校验,结构化 schema 引导正确传参

#### 3.3 MCP 生态与供应链安全

**MCP 三原语:** 工具(模型可执行操作)/ 资源(应用可读取数据,只读)/ 提示模板(用户可选用)。资源与工具分离使 Agent 区分"获取信息"和"执行操作"。

**供应链安全(传统软件供应链一脉相承):**

| 层 | 措施 |
|---|---|
| 上下文层 | 接入前审查 description——当作**不可信输入**审计,而非无害元数据;锁版本拒绝静默更新,升级重新审查 |
| 执行层 | 每服务器配最小权限凭证——即使描述骗过模型,凭证也限制它真正能做到的事 |
| 运行时 | **Sidecar 机制**——独立安全审查模型只看结构化工具调用数据,不易被藏在描述里的话术操纵 |

Simon Willison 致命三要素:为评估一个 MCP 工具组合的整体风险提供系统框架。

#### 3.4 执行工具安全机制(层次化)

**事后验证的模态切换(关键洞察):** 不是让第二个模型重读相同内容再审,而**在不同模态下检验**——代码生成文档→渲染为视觉输出检查排版;修改配置文件→沙盒实际运行验证生效。不同模态提供互补验证视角,单一模态审查易陷相同盲区。提议者-审核者:Proposer 生成 PPT 代码,Reviewer 检查渲染截图。

**沙盒隔离:** 通用执行工具(Python/Shell)需沙盒与宿主机隔离。
> ⚠️ **关键澄清:venv 不是沙盒。** venv 只隔离包依赖,对文件系统、网络、进程无任何安全约束——venv 中代码照样可删任意文件、访问任意网络。

**可观测性:** 日志(时间/参数/结果/耗时)+ 审计追踪(谁在什么上下文为何执行)+ 性能指标(频率/成功率/耗时)+ 告警(频繁失败/超时/资源超限)。

**HITL(人在回路):** 超时阈值 + 默认行为("5 分钟无响应采保守策略")+ 优先级队列(紧急多渠道通知,普通只发邮件)。

### 四、交互扩展与系统原语(第6章)

#### 4.1 异步事件驱动

**事件循环:** 异步 Agent = 长期运行循环——每轮从输入队列取若干事件→追加轨迹→调一次 LLM→执行工具→回循环开头等下一批。与 Go 的 goroutine 从 channel 读消息、`for { select { ... } }` 逐轮处理是同一结构。

**Heartbeat / 定时器 = 主动服务的根源:**
- Heartbeat:每 N 分钟唤醒检查需关注事项
- 一次性定时器:明确时间点(周六→设"下周一 10:00 致电银行",触发自动拨打)
- 循环定时器:周期性(每小时查服务器健康)/不支持推送的服务反复查询进展

**事件优先级动态判断:** 不是所有事件同等重要——取消当前操作(紧急)/加入队列(常规)/并行处理(独立轻量查询)。

#### 4.2 PineClaw:真实经济任务案例

Pine AI 代替用户拨真实电话(协商账单、取消订阅、处理保险理赔)。通话过程中可能随时需要用户介入——典型的异步事件驱动 + HITL 场景。这正是全书架构原则的来源:长流程、高风险、单步错误造成实际损失,逼出 Harness/Skill/提议者-审核者等工程方法。

#### 4.3 四类场景共享的系统原语

异步事件 / 语音 / Computer Use / 机器人操作四类场景共享:**唤醒、安全点、取消、抢占、快慢路径分离**。动作分块(一次规划一小段,可被抢占)是跨场景通用机制。

### 五、实践建议(综合全书)

1. **通用 Agent 起步:** Coding Agent + 文件系统,七个简单工具(read/write/grep/glob/shell/code_interpreter/…),缺口靠代码生成现场补
2. **工具取舍:** 默认通用工具;仅安全/权限/审计、平台差异、高频、复杂参数四情况用专用工具
3. **安全三防线:** 上下文层(description 当不可信输入审计 + 锁版本)+ 执行层(最小权限凭证)+ 运行时(Sidecar 独立审查);执行用真沙盒(非 venv)
4. **验证:** 外部独立验证器(隐藏测试/状态断言/外部终止钩子),不靠模型自报;事后验证用模态切换
5. **记忆:** Markdown + Git 优先于向量 DB(可读可改、保时间序、可回滚)
6. **交互:** 事件循环 + Heartbeat 实现主动服务 + 事件优先级动态判断 + HITL 超时默认保守
7. **护栏靠下:** 上下文层→执行层→数据层,越靠下越难穿透;数据层(行级安全/约束校验)不依赖上两层是否正确

### 第4轮小结

- **案例主线:** Pine/PineClaw(真实经济任务,架构原则来源)、OpenClaw(Coding+文件系统架构)、Claude Code(Harness 范本)三案例贯穿全书
- **Coding Agent:** 通用 Agent 核心 = Coding Agent + 文件系统;代码生成是元能力(六方向由内向外);Markdown+Git 优于向量 DB
- **工具安全:** 五类工具 + 形态/披露两决策;默认通用四例外专用;MCP 供应链安全三层;执行安全=模态切换验证 + 真沙盒(venv≠沙盒)+ 可观测性 + HITL
- **交互扩展:** 事件循环(同 Go goroutine)+ Heartbeat/定时器(主动服务根源)+ 事件优先级;四场景共享唤醒/安全点/取消/抢占/快慢路径原语
- **一句话实践:** 把可靠性建在模型之外——Harness(约束/验证/纠正)+ 文件系统(可回滚记忆)+ 外部独立验证 + 数据层护栏兜底

---

## 第5轮:综合评价与建议

> 终轮:综合前4轮,评价优缺点、对比同类、给阅读建议与研究方向

### 一、书评:优点

| # | 优点 | 依据 |
|---|---|---|
| 1 | **工程实践导向,非跑通 demo** | 来自 Pine 真实高风险任务(账单协商/退款/理赔),原则经长流程检验 |
| 2 | **核心公式简洁有力** | `Agent = LLM + 上下文 + 工具`,三层等价表述(直觉大脑眼睛手脚 / 实现层 / 学术 RL)贯穿全书 |
| 3 | **结论有消融实证支撑** | 结构打乱降 30%、工具描述移除错误率 +45%、KV Cache 字节级稳定性——非主观论断 |
| 4 | **概念权重量化** | 跨章频次表(评估320/Harness210/Skill195)揭示重心,非拍脑袋 |
| 5 | **章节独立 + 多阅读路径** | 对开发者/时间有限/训练方向/产品架构师分别给路径 |
| 6 | **立场鲜明** | "实践在前,命名在后";好原则穿越模型迭代周期 |
| 7 | **配套开源代码 + 分级实验** | 难度 ★/★★/★★★,强烈建议动手跑 |

### 二、书评:局限

| # | 局限 | 说明 |
|---|---|---|
| 1 | **偏架构方法论,模型/API 细节少** | 适合架构师;初学者需补 LLM 基础(作者推荐《图解大模型》互补) |
| 2 | **训练章门槛较高** | 第8章需 ML/数学基础(训练/损失/梯度/概率统计) |
| 3 | **案例来源集中** | 多来自 Pine/作者团队,第三方独立验证案例相对少(趋同性论述为主) |
| 4 | **多 Agent 篇幅偏前瞻** | 第10章(频次81)讨论分类框架 + Agent 社会/经济,落地案例不及前几章扎实 |
| 5 | **领域时效性** | 产品/模型信息截至 2025 年中,快速演进领域有保鲜压力 |

### 三、对比同类

| 对比对象 | 定位差异 |
|---|---|
| 《大模型应用开发指南》类 | 那类偏 API 调用/单轮;**本书重 Agent 系统架构与可靠性** |
| LangChain/LlamaIndex 文档 | 那类偏框架使用(怎么调 API);**本书讲为什么这样设计** |
| 学术 RL/Agent 论文 | 那类偏理论推导;**本书工程视角,RL 形式化只作映射不深推** |
| 《图解大模型》(本书自荐互补) | 那书讲 Transformer/预训练微调基础;**本书讲 Agent 工程**——底层 + 工程互补 |

**独特定位:** 以可解释、可验证、可复用的工程原则设计 Agent,源自生产系统实践而非演示系统——市面上少见的"Agent 架构方法论"著作。

### 四、阅读建议(学习路线图)

```
                        ┌─ 全书顺序(开发者主线)
                        │   1-6 构建 → 7-10 提升
                        │   重点: Ch1 全局 + Ch2 上下文(最关键)
读者 ─┬─ Agent 开发者 ──┘
      │
      ├─ 时间有限 ─────── Ch1 + Ch2(KV Cache 原理可跳过,只记三结论)
      │
      ├─ 模型训练方向 ─── Ch7 评估 + Ch8 后训练(评估是训练前提)
      │                  先读 Ch1-2 建认知
      │
      ├─ 产品/架构师 ─── Ch1 + Ch2 + Ch4 工具 + Ch5 Coding + Ch6 交互
      │                  跳过 Ch8 训练细节
      │
      └─ 安全方向 ─────── Ch2 提示注入 + Ch4 工具安全 + 护栏三层(Ch1) + Ch6 原语
```

**配套:** 动手跑实验(作者强烈建议);用 Claude Code/Codex/Cursor 体验 ReAct 循环、工具调用、上下文管理——第一手体验对理解设计原则极有价值。

### 五、研究方向(从全书提炼)

1. **Harness 工程化** — 模型能力商品化后,竞争优势转移到模型之外;约束/验证/纠正的系统化方法待沉淀
2. **评估闭环** — LLM-as-Judge(Rubric + 防长度偏差)+ 失败归因 + 轨迹回流;从经验判断到可复现工程
3. **SFT 记忆 / RL 泛化** — 协变量漂移是分水岭;数据与环境 > 算法;RL 推高已有低概率而非从零创造
4. **持续进化** — 四种更新载体(便宜优先)+ 睡眠学习 + 经验转能力闭环
5. **多 Agent 协作** — 分类框架(上下文共享/独立 × 对等/管理者/去中心化)+ Agent 社会/经济(前瞻方向)
6. **代码生成元能力** — 动态创造工具/约束/表达,六方向由内向外
7. **交互扩展** — 异步事件驱动 + 语音 + Computer Use + VLA,共享系统原语(唤醒/安全点/取消/抢占/快慢路径)
8. **安全** — 护栏三层(越靠下越难穿透)+ MCP 供应链 + 提示注入攻防

### 六、核心收获(三句话)

1. **Agent = LLM + 上下文 + 工具,可靠性建在模型之外(Harness)**——约束、验证、纠正决定能不能可靠做事
2. **评估是工程化的前提**——没有评估,改动提升与随机波动不可区分;先测量再优化
3. **实践在前,命名在后**——好的架构原则穿越模型迭代周期,描述的是智能系统与世界交互的基本模式

### 第5轮小结

- **书评:** 优点在工程实践导向 + 实证支撑 + 量化重心;局限在偏架构少细节、训练章门槛、案例集中、多 Agent 偏前瞻、领域时效
- **定位独特:** 市面少见的"Agent 架构方法论"著作,与《图解大模型》底层互补
- **路线图:** 按读者类型分五条路径,核心是 Ch1 全局 + Ch2 上下文,配套动手跑实验
- **研究方向:** 八个方向,Harness 工程化与评估闭环是最具迁移性的两个

---

## 全书总评

本书以一句话公式 `Agent = LLM + 上下文 + 工具` 为轴,用 10 章完成"构建(1-6)+ 提升(7-10)"的完整论述。最大价值不在教某个模型或框架的用法,而在提炼**穿越模型迭代的架构原则**——这些原则来自 Pine 等生产系统在长流程高风险任务中反复验证,并以消融实验和量化频次支撑,具有方法论级别的迁移性。

对从业者:**先建立评估(测量先行),再把可靠性建在模型之外(Harness),用四种载体持续进化,守住安全边界(护栏三层靠下)**——这是一条可落地的工程路径。对研究者:协变量漂移、RL 推高与创造的边界、多 Agent 社会经济等方向仍有大量开放问题。

实践在前,命名在后。掌握"为什么这样设计",方能在技术浪潮变化中保持清醒判断力。

---

*分析完成:5 轮递进(结构概览 → 技术体系 → 工程实践 → 案例应用 → 综合评价),配套 epub 文本提取与定向检索,每轮校验 token 节省插件效果。*

---

## 补充:第10章 多 Agent 协作深度分析

> 前5轮第10章仅在结构概览提及(频次81),本节补全。重启后于插件验证场景下完成。

### 一、分类框架:两维度 × 通信即 IPC

**维度一:上下文是否共享**
- **共享**=继承完整轨迹(换提示词/工具集即新 Agent,但保留记忆);信息不丢,但上下文膨胀
- **独立**=各自上下文,通过显式通信;模块化隔离好,易扩展,新增 Agent 不改现有逻辑

**维度二:协作拓扑:** 对等 / 管理者 / 去中心化

**通信机制 = IPC 两范式:**
- 共享内存 → 共享文件系统(Agent 世界的"共享内存")
- 消息传递 → 工具调用参数(同步)/ 消息总线(异步)
- Go 箴言:"不要通过共享内存来通信,而要通过通信来共享内存"

### 二、核心判据:信息增量(多 Agent 何时优于单 Agent)

**唯一判据:协作是否引入生成时无法获得的新信息**

| 模式 | 新信息 | 效果 |
|---|---|---|
| 同模型自审(重读自己输出) | 否 | 无效甚至有害 |
| 不同 Agent 辩论同段文本 | 否 | 等计算量下与单 Agent 持平 |
| 审核者用测试执行结果 | 是(执行反馈) | 显著提升 |
| 审核者看渲染截图 | 是(视觉反馈) | 显著提升 |
| 审核者用外部工具验证 | 是(工具反馈) | 显著提升 |

- **Huang ICLR2024:** GPT-4 无外部反馈自审,准确率反降(改错 > 改对)
- **矛盾解释:** 学术研究(同上下文讨论,无新信息)vs 工程实践(含外部反馈环路,有新信息)
- **数据处理不等式:** 串行传递中间结论只可能丢失信息、不可能凭空创造

### 三、Loop 工程:解决过早终止

**过早终止三形态:** 偷懒假完成(测试没跑就报告完成)/ 过早放弃(一个渠道被拒就宣布办不成)/ 假成功(口头同意退款但用户 App 确认步未走完)→ **根源:验证前"完成"只是模型宣称,不是证明**

- **Loop 工程:** 验证器而非模型判定"能否停";Boris Cherny:"我的工作是写 loop"
- **瓶颈在验证器不在模型**——验证不可靠,循环转得再快也只是更快标记劣质产出为完成
- **LoopX:** 决策→执行→独立验证→提交;**模型可提出"完成",但不能批准自己的"完成"**
- **LongHorizon-Harness:** Manage-Execute-Audit;WeaveBench 51.8%→80.7%(代价 2.3 倍 token)

### 四、对等协作五模式

1. **提议者-审核者(最经典):** 审核者读**独立证据**非复述提议者解释;退回须给可定位修复条件;审核者不能修改测试/证据采集器/发布门槛(否则"独立验证"退化为自我批准)
2. **辩论(有争议):** 数据处理不等式→等计算量下单 Agent 持平甚至更好;收益可能来自多消耗总计算量
3. **头脑风暴:** 独立生成再相互激发,探索更广阔解空间
4. **专家小组:** 跨学科互补(非对抗),拼出问题全貌
5. **对等迭代改进循环:** 2-3 Agent 固定拓扑多轮迭代(论文起草者+评论者)

### 五、管理者模式:中心化协调

- **Manager = 项目经理:** 拆解/调度/跟踪/整合;子 Agent 建模为可调用工具(调用 Agent 与调用普通工具无本质区别)
- **单点瓶颈:** 弱规划者是系统最关键瓶颈(Plan-and-Act:WebArena 54% 靠改善 Planner 非 Executor)→ **最强模型给 Manager(规划者),而非均分**
- **结算点:** "第一个已验证成功"非"第一个声称成功";`settle_once` 须幂等(锁/事务保护)
- **顺序 vs 并行:** 并行需消息总线(公共公告板,发布/订阅,异步不阻塞)
- **案例:** 书籍翻译 Agent(Glossary/Translation/Proofreading/Manager,上下文隔离各司其职);电话+电脑 Agent(Computer 编排+Phone 收集,无需额外 Manager)
- **灵台:** 主器灵(Manager)/分神(短时并行工作者)/分身(持久专门化);器灵即其文件;凝蜕=上下文压缩

### 六、虚拟文件系统四区域(数据平面)

**多 Agent = 操作系统 LLM 版(Actor 模型)。** 文件系统四区域 = 权限各异的内存段:

| 区域 | 可见性 | 读写 | 并发控制 |
|---|---|---|---|
| Agent 专属工作区 | 仅该 Agent | 读写 | 不需要(私有) |
| 多 Agent 共享空间 | 所有 Agent + 用户 | 读写 | 乐观锁 / worktree |
| 外部挂载资源 | 视外部授权 | 多为只读 | 外部源负责 |
| 系统内置资源 | 所有 Agent | 只读 | 不需要(只读) |

**文件路径作为通用接口:** 传递轻量路径字符串而非将内容载入上下文窗口

### 七、控制平面四原语(对应操作系统)

1. **消息传递:** 点对点(拓扑固定)/消息总线(异步并行);结构化信封(发送者/目标/类型/JSON 负载)保证可路由可追溯
2. **状态查询:** 消息(异步问答,不阻塞)/共享文件(轨迹持久化=读进程内存,但费 token→约定进度文件 progress.md);最后修改时间超 N 分钟→判定卡住
3. **执行终止:** SIGTERM 优雅(安全点清理,返回 ack)vs SIGKILL 强制(兜底);**级联取消**(Go context,取消父级→子级全取消);nohup=context.Background()
4. **资源调度:** token/并发额度稀缺;Manager 具推理能力可动态分配(赛马机制——并行探索,据进展决定给谁加资源/终止谁)

### 补充小结

- **多 Agent = 操作系统 LLM 版:** 数据平面(文件系统四区域)+ 控制平面(消息/状态/终止/调度四原语)
- **核心判据:信息增量**——引入新信息(执行/视觉/工具反馈)→有价值;无新信息(自审/同文本辩论)→不如单 Agent,自审反而有害
- **Loop 工程:验证器是瓶颈**——模型可提出完成但不能批准完成;最强模型给 Manager(规划者)非均分
- **与前轮呼应:** 提议者-审核者(Ch3/4/5/7/9/10 贯穿五大设计模式之首)、Loop 工程(Ch1 五层范式的 Loop 层)、外部独立验证(Ch7 评估原则)、子 Agent 返回结构化摘要(Ch4)
