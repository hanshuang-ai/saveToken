{
  "description": "frugal: conservative Bash/Read output views with committed original retrieval",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|Read",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/node_modules/tsx/dist/cli.mjs\" \"${CLAUDE_PLUGIN_ROOT}/hooks/post-tool-compress.ts\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
